% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_vacancies.R
\name{get_vacancies}
\alias{get_vacancies}
\title{Get Vacancies from ADEM Database}
\usage{
get_vacancies(skill = NULL, company = NULL, canton = NULL, limit = 100)
}
\arguments{
\item{skill}{Character. Optional. Filter vacancies by required skill label (e.g., "German").}

\item{company}{Character. Optional. Filter vacancies by company name (e.g., "Company_2").}

\item{canton}{Character. Optional. Filter vacancies by canton (e.g., "Luxembourg").}

\item{limit}{Integer. Number of rows to return. Default is 100.}
}
\value{
A data frame containing the vacancies that match the filters, or \code{NULL} if no matches are found.
}
\description{
Retrieves job vacancies from the ADEM database with optional filtering
by skill, company name, and canton. Returns a data frame with the selected
vacancy details.
}
\details{
The function connects to the ADEM database, performs the query with any applicable filters,
and returns a data frame with columns such as \code{vacancy_id}, \code{company_id}, \code{occupation},
\code{canton}, \code{year}, \code{month}, \code{skill_label}, and \code{company_name}. If all filter parameters are \code{NULL},
it retrieves the latest \code{limit} vacancies without filtering.
}
\examples{
# Get 50 vacancies requiring Python in Luxembourg canton
get_vacancies(skill = "Python", canton = "Luxembourg", limit = 50)

# Get all vacancies for the company "Amazon"
get_vacancies(company = "Amazon")

# Get vacancies without any filters (default limit = 100)
get_vacancies()
}
