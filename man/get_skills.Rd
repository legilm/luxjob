% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_skills.R
\name{get_skills}
\alias{get_skills}
\title{Retrieve Skills from ADEM Database}
\usage{
get_skills(limit = 100)
}
\arguments{
\item{limit}{Integer. The maximum number of skill records to retrieve. Default is 100.}
}
\value{
A data frame containing skill records from the ADEM database.
}
\description{
This function connects to the ADEM database and retrieves a list of skills
from the \code{skills} table, ordered by \code{skill_id}. The number of records
returned can be limited using the \code{limit} parameter.
}
\details{
The function uses a database connection established through \code{connect_db()} and performs
a SQL query using \code{glue_sql()} to safely inject the \code{limit} parameter. The connection
is closed automatically after the query is executed.
}
\examples{
\dontrun{
# Retrieve default 100 skill records
get_skills()

# Retrieve only 10 skill records
get_skills(limit = 10)
}
}
\seealso{
\code{\link[=connect_db]{connect_db()}}, \code{\link[DBI:dbGetQuery]{DBI::dbGetQuery()}}, \code{\link[glue:glue_sql]{glue::glue_sql()}}
}
